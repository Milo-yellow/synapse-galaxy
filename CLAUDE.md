# CLAUDE.md — 시냅스 은하 작업 안내

클로드가 이 저장소에서 작업할 때 먼저 읽는 문서.

## ⚠️ 먼저 확인할 것 — 파일이 두 벌이다

**「속삭이는 서랍」의 진짜 소스는 이 저장소의 `seorap.html` 하나뿐이다.**

`OneDrive/AI_제작/서랍만들기/서랍.html` 이라는 **떨어져 나온 옛 사본**이 로컬에 있다.
git에도 없고 배포도 안 되고, 내용도 뒤처져 있다. **거기를 고치면 웹에 아무 일도 안 일어난다.**

2026-08-08에 실제로 그 함정을 밟았다 — 통계 탭을 사본 쪽에 만들어놓고 "왜 사이트에 안 뜨지"가 됐다.

> 파일을 열었다고 원문을 편 게 아니다. **어느 원문인지**를 먼저 확인할 것.

그리고 그 사본에는 **API 키가 하드코딩돼 있다.** 통째로 복사해 올리면 공개 저장소에 키가 박힌다.
반드시 **변경분만 이식**할 것.

## 구조

```
index.html      은하 본체 (글·댓글, Supabase에서 CSR로 그림)
seorap.html     🗄️ 속삭이는 서랍 — 꿈 노트 앱 (별도 페이지)
build.mjs       빌드 시점 정적 스냅샷 생성기 (크롤러·링크 미리보기용)
dist/           빌드 산출물. 직접 고치지 말 것 — push 때 자동 생성된다
supabase/       Edge Functions (claude-gate 등)
claude-gate.md  클로드 전용 우체통 사용법
```

## 배포

**push하면 GitHub Actions가 `build.mjs`를 돌려 `dist/`를 만들고 Pages에 올린다.**
따로 빌드해서 커밋할 필요 없다. 반영까지 대략 30초~1분.

## 서랍 (`seorap.html`)

읽기 전용 배포판이라 글쓰기 버튼(`fab`)과 휴지통 버튼은 `display:none`으로 숨겨져 있다.

**API 키는 소스에 없다. URL 해시로 받는다.**

```js
var HASH = new URLSearchParams(location.hash.replace(/^#/, ""));
var KEY  = HASH.get("k") || "";   // 읽기
var WKEY = HASH.get("w") || "";   // 쓰기
```

→ `…/seorap.html#k=<읽기키>&w=<쓰기키>`
**어떤 키도 소스에 적지 말 것.**

### 화면 구성

| 버튼 | 하는 일 |
|---|---|
| 📊 통계 | 공백 지표·연도별·최근 24개월·올해 공백 목록 |
| 📅 날짜별 | 날짜 묶음 보기 ↔ 카드 보기 토글 |
| 🗑 휴지통 | (배포판에선 숨김) |

### 통계 탭 (2026-08-08 추가)

- `computeStats()` — 현재 토픽 필터(`topicPool()`) 기준으로 계산한다. `꿈` 칩을 누르면 꿈만 집계.
- **공백**은 "연속 기록일 사이의 빈 날 수". `GAP_MIN = 5`가 "긴 공백" 기준.
- 지표: 지금 며칠째 비었나 / 올해 그만큼 이상 비었던 횟수 / 올해 최장 / 최근 1년 빈도
- 연도별 막대는 **노트 수**, 밑줄 설명은 **실제로 적은 날 수**. 둘을 구분해서 보여주는 게 중요하다
  (노트 수는 기록 습관에 흔들리고, "적은 날 수"가 실제 신호에 가깝다).
- 날짜는 `YYYY.MM.DD` 문자열이고, 오늘 판정은 KST 기준(`todayKST()`)이다.

### 날짜별 보기 레이아웃

`.dlist`는 **CSS Grid**다. 멀티컬럼(`columns`)으로 하면 안 된다 —
읽는 순서가 세로로 꺾여서, 1열 끝까지 내려갔다가 다시 위로 올라가야 2열이 보인다.
**시간순으로 훑는 화면이라 좌→우, 그다음 아랫줄이 맞다.** 대신 줄마다 높이가 달라 여백이 좀 생기는데,
순서를 지키는 대가로 받아들인 것.

## 손대기 전에

- `seorap.html`은 빌드 단계가 없는 **단일 파일**이다. 바닐라 JS(`var`, `function`), 프레임워크 없음. 그 스타일을 유지할 것.
- 고친 뒤 스크립트 문법 검사:
  ```bash
  node -e "const t=require('fs').readFileSync('seorap.html','utf8');
  new (require('vm').Script)(t.slice(t.lastIndexOf('<script>')+8, t.lastIndexOf('</'+'script>')));
  console.log('OK')"
  ```
- 커밋 메시지는 기존 스타일대로 `서랍: ~` / 한 줄 한국어.
